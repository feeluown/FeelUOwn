fuocore usage example
================

Usage examples are listed there.

In addition, `shell.py` sets up a typical usage scenario. Developers can run
`ipython -i shell.py` to perform interactive testing.
